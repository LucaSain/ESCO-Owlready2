# Test-only dependencies. Install with:  pip install -r requirements-dev.txt
# (requirements.txt is installed separately; this layers on top of it.)
pytest==9.1.1
httpx2==2.12.0  # starlette.testclient hard-requires it; without it every TestClient import raises
